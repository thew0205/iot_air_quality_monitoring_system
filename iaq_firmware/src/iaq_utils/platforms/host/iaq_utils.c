#include <stdlib.h>

void iaq_exit()
{
    // Perform any necessary cleanup or resource deallocation here
    // For example, close files, free memory, etc.

    // Exit the program with the provided status code
    exit(EXIT_FAILURE);
}